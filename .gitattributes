# 換行正規化: repo 內一律存 LF, 避免 Windows/WSL 兩邊 git 對同一
# working tree 判定不一致 (autocrlf 差異會把所有檔案標成 modified)
* text=auto

# bash script 必須是 LF (CRLF 會讓 bash 報 '\r' 錯誤)
*.sh text eol=lf

# PowerShell script 用 CRLF
*.ps1 text eol=crlf
